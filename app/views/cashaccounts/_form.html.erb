<%= form_with(model: cashaccount) do |form| %>
  <% if cashaccount.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cashaccount.errors.count, "error") %> prohibited this cashaccount from being saved:</h2>

      <ul>
        <% cashaccount.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.number_field :amount %>
  </div>

  <div class='mt-2'>
    <%= form.submit %>
  </div>
<% end %>
